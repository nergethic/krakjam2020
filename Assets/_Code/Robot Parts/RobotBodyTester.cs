using UnityEngine;

namespace _Code.Robot_Parts {
    public class RobotBodyTester : MonoBehaviour {
        public BodyPart.BodyType toFind;

        [ContextMenu("gdsf")]
        void Test() {
//            var body = FindObjectOfType<RobotBody>();
//            var (a, b) = body.GetBodyPart(toFind);
//            if(a)
//                Debug.Log(b.name);
        }
    }
}
